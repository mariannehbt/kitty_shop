<%= form_with(model: items_image, local: true) do |form| %>
  <% if items_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(items_image.errors.count, "error") %> prohibited this items_image from being saved:</h2>

      <ul>
      <% items_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
